import React from 'react';
//this will be the page after login. This page is just for refrence
const Hero =({handleLogout}) => {
    return(
        <section className="hero">
            <nav>
                <h2>Welcome</h2>
                <button>Logout</button>
            </nav>

        </section>
    )
};
export default Hero;